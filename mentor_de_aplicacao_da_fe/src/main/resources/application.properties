# ARQUIVO: /resources/application.properties
# **SEGURO PARA O GIT:** Contém apenas PLACEHOLDERS e configurações de ambiente.
# Este arquivo DEVE ser versionado.

# Perfil padrão se nenhum for especificado
spring.profiles.active=prod

# Configuração do PostgreSQL Local
# As credenciais são carregadas do application-local.properties ou variáveis de ambiente.
# spring.datasource.url=jdbc:postgresql://localhost:5432/mentor_db
# spring.datasource.username=${DB_USER_DEV}
# spring.datasource.password=${DB_PASS_DEV}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Email de TESTE (Carregadas externamente)
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=${MAIL_USER_DEV}
#spring.mail.password=${MAIL_PASS_DEV}

# Chaves de API (Carregadas externamente)
#gemini.api.key=${GEMINI_API_KEY_DEV}
#mercadopago.access.token=${MP_ACCESS_TOKEN_DEV}

# URL de Desenvolvimento/Teste (Ngrok)
#server.port=8080
#app.base.url=https://aerologic-clasping-lizabeth.ngrok-free.dev

# Outras configurações
# A propriedade 'mercadopago.external.reference' é obsoleta e foi removida do uso no código Java.
# mercadopago.external.reference=semente-plano-

#Teste modificações feitas